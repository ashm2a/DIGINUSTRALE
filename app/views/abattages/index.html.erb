<div class="container">
  <h1>Lot abattage</h1>
    <ul>
      <% @abattages.each do |abattage| %>
       <a href="<%= abattage_path(abattage) %>">
        <li><%= abattage.numéro_lot %></li>
        <li><%= abattage.date %></li>
        <li><%= abattage.lieu %></li>
      <% end %>
    </ul>
</div>

<%= link_to "Exit", "/porcs", class: "btn btn-primary" %>
